/**
 * Centralized API configuration
 * Use this constant instead of hardcoding URLs in slices and API functions
 */

export const API_BASE = "http://localhost:8080/api";

export default {
  API_BASE,
};
