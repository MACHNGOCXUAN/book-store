export interface Book {
  bookId: string;
  title: string;
  author: string;
  publisher: string;
  category: string;
  price: number;
  stock: number;
  description: string;
  publishDate: string;
  coverImage: string;
}
